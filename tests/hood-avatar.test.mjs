import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import {GLTFLoader} from 'three/addons/loaders/GLTFLoader.js';
test('world avatar exposes a hand attachment and all gameplay clips after Three.js import',async()=>{const raw=fs.readFileSync('static/cryptodoodz/models/0001.glb');const buffer=raw.buffer.slice(raw.byteOffset,raw.byteOffset+raw.byteLength);const gltf=await new GLTFLoader().parseAsync(buffer,'');assert.ok(gltf.scene.getObjectByName('hand.R')??gltf.scene.getObjectByName('handR'));assert.deepEqual(gltf.animations.map(a=>a.name).sort(),['Idle','Jump','Run','Shoot','Walk','Wave']);});
